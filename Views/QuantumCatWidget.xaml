<Window x:Class="DesktopCompanions.QuantumCatWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DesktopCompanions.ViewModels"
        Title="Quantum Cat" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="50"
        Height="50">
    
    <Window.Resources>
        <!-- Sprite sheet animations are handled in code-behind -->
    </Window.Resources>

    <Grid>
        <!-- Drag Handle Area (10px border) -->
        <Border x:Name="DragHandle" 
                Background="Transparent" 
                MouseLeftButtonDown="DragHandle_MouseLeftButtonDown"
                MouseMove="DragHandle_MouseMove"
                MouseLeftButtonUp="DragHandle_MouseLeftButtonUp"
                Cursor="SizeAll">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Margin" Value="-10"/>
                    <Setter Property="Padding" Value="10"/>
                </Style>
            </Border.Style>
            
            <!-- Cat Display Area -->
            <Viewbox Stretch="Uniform">
                <Canvas Width="64" Height="64" ClipToBounds="True">
                    <!-- Cat Icon Display -->
                    <Image x:Name="CatIconImage" 
                           Width="32" Height="32"
                           RenderTransformOrigin="0.5,0.5"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                        <Image.RenderTransform>
                            <TranslateTransform/>
                        </Image.RenderTransform>
                        <Image.ToolTip>
                            <ToolTip>
                                <StackPanel>
                                    <TextBlock Text="Quantum Cat Widget" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <TextBlock>
                                        <Run Text="CPU Usage: "/>
                                        <Run Text="{Binding CpuLoad, StringFormat={}{0:F1}%}" FontWeight="Bold"/>
                                    </TextBlock>
                                    <TextBlock>
                                        <Run Text="RAM Usage: "/>
                                        <Run Text="{Binding RamUsage, StringFormat={}{0:F1}%}" FontWeight="Bold"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding AnimationStateName}" Margin="0,5,0,0" FontStyle="Italic"/>
                                </StackPanel>
                            </ToolTip>
                        </Image.ToolTip>
                    </Image>
                </Canvas>
            </Viewbox>
        </Border>
    </Grid>
</Window>
